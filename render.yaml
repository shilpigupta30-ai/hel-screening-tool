services:
  - type: web
    name: hel-screening-tool
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run crp_final_v12.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
    envVars:
      - key: NOAA_CDO_TOKEN
        sync: false
